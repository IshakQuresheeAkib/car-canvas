const ColorTitle = ({children}) => {
    return (
        <span className='xl:text-6xl text-5xl bg-gradient-to-bl bg-clip-text text-transparent from-darkRed to-lightRed font-bold'>{`  ${children}`}</span>
    )}
export default ColorTitle;